# package.json declara el rango de Node en "engines", pero npm no lo hace cumplir salvo que
# se le pida. Sin esto, instalar con una version vieja no falla: produce errores raros mas
# adelante, en el build o en tiempo de ejecucion, lejos de la causa.
engine-strict=true
